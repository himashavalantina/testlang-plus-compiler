package org.example;

// The interface must have a body, even if it's empty
public interface AssertionStatement extends TestStatement {
    // No members needed, but the braces are required
}
// No semicolon needed here for modern compilers